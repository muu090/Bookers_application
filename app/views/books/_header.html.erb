<%# 共通ヘッダー部分-Start- %>
  <body>
    <header class="navbar navbar-fixed-top navbar-inverse">
      <div class='container'>
        <nav class='nav navbar-nav navbar-left' >
          <span>Bookers</span>
        </nav>
        <nav>
          <ul class="nav navbar-nav navbar-right">
            <% if user_signed_in? %>
              <li>
                <%= link_to "Home", user_path(current_user.id), {class: "glyphicon glyphicon-home home"} %>
              </li>
              <li>
                <%= link_to 'Users', users_path, {class: "glyphicon glyphicon-user users"} %>
              </li>
              <li>
                <%= link_to 'Books', books_path, {class: "glyphicon glyphicon-book books"} %>
              </li>
              <li>
                <%= link_to "logout", destroy_user_session_path, method: :delete %>
              </li>
            <% else %>
              <li class="glyphicon glyphicon-home home">
                <%= Home %>
              </li>
              <li class="glyphicon glyphicon-link about">
                <%= About %>
              <li>
                <%= link_to "sign up", new_user_registration_path, {class: "glyphicon glyphicon-edit sign_up"} %>
              <li class="">
                <%= link_to "login", new_user_session_path, {class: "glyphicon glyphicon-edit sign_up"} %>
              </li>
            <% end %>
          </ul>
        </nav>
      </div>
    </header>
<%# 共通ヘッダー部分-Finish- %>